# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
*.iml

# Android (generato da bootstrap.yml la prima volta; committato di
# proposito per poterlo modificare a mano in seguito — vedi
# docs/ARCHITETTURA.md — quindi NON ignorato qui)
# android/.gradle/ e simili artefatti locali restano comunque esclusi:
android/.gradle/
android/local.properties
android/app/debug
android/app/profile
android/app/release
android/**/gradle-wrapper.jar

# iOS (se aggiunto in futuro)
ios/Flutter/.last_build_id
ios/Pods/

# macOS/editor
.DS_Store
.vscode/
.idea/

# Log locali di `flutter run` (es. flutter_01.log) — non vanno committati
flutter_*.log
